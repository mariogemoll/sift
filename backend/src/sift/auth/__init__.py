"""Getting in: the passphrase, the session tokens it signs, and the gate that checks them.

Deliberately empty, so importing the pure `passphrase` module does not pull in
FastAPI by way of `gate`.
"""
